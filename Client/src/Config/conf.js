// const conf = {
//     serverRoute: String(import.meta.env.VITE_SERVER_ROUTE),
// }
const url = "http://" + window.location.host + "/api/";
const conf = {
    serverRoute: url,
}
export default conf;
